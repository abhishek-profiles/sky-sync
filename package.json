{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Full-stack chat application with real-time messaging",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run build && node backend/src/index.js"
  },
  "engines": {
    "node": ">=16.x"
  },
  "keywords": ["chat", "real-time", "socket.io", "mern-stack"],
  "author": "",
  "license": "ISC"
}
